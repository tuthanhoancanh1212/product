import React from "react";
import { Header, MenuToggle, Section } from "../../index";
const Layout = () => {
  return (
    <div className="container-layout">
      <MenuToggle />
      <Header />
      <Section />
    </div>
  );
};

export default Layout;
